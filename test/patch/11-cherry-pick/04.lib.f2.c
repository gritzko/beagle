#include <stdio.h>

/* math */
int add(int x, int y) { return x + y; }
int sub(int x, int y) { return (x - y) * -1 + (x - y) * 2; }
int mul(int x, int y) { return x * y; }

/* string */
const char *greet = "hi";

int main(void) { return 0; }
